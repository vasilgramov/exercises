package codeChef.contests.aprilChallenge2017;

import java.util.Deque;
import java.util.Scanner;

public class cc_bearAndCliqueDistances {

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);

        int tests = Integer.parseInt(in.nextLine());
        for (int i = 0; i < tests; i++) {
            String[] tokens = in.nextLine().split("\\s+");

            int citiesCount = Integer.parseInt(tokens[0]);
            int oldRoads = Integer.parseInt(tokens[1]);
            int lengthOfOldRoads = Integer.parseInt(tokens[2]);
            int newRods = Integer.parseInt(tokens[3]);
            int startPoint = Integer.parseInt(tokens[4]);

        }
    }
}
